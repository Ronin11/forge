#!/bin/bash
# a coder that must never be invoked: `file_into_initiative` should end the
# task at the investigate step and never reach the code step
echo "the code step ran, and it should not have" >&2
exit 1
